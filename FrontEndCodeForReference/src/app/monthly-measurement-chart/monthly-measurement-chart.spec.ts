import { MonthlyMeasurementChart } from './monthly-measurement-chart';

describe('MonthlyMeasurementChart', () => {
  it('should create an instance', () => {
    expect(new MonthlyMeasurementChart()).toBeTruthy();
  });
});
